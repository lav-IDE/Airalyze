# In production point this to the deployed FastAPI origin, without a trailing slash.
# Leave unset when the frontend and API are served behind the same /api reverse proxy.
VITE_API_BASE_URL=https://api.example.com
